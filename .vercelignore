.android-tools/
android/
android-twa/
node_modules/
*.aab
# Root-anchored on purpose: the build script leaves a copy of the signed APK at
# the repo root which must not be uploaded, while the published one under
# public/downloads/ must be. A bare *.apk would exclude both and 404 the
# download link.
/*.apk
